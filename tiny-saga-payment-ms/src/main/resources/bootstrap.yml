server:
  port: 8087
  
springdoc.api-docs.path: /payment-docs

spring:
  kafka:
    bootstrap-servers: localhost:9092 # For docker use port 29092, For windows cluster use port 9092
    group_id: payment-ms

walletMs:
  host: http://wallet-ms:8086
  basePath: /v1/api/wallet
  
isChoreographyEnabled: false
  